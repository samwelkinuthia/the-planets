<%= form_for [@galaxy, @star] do |form| %>
  <% if star.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@galaxy.star.errors.count, "error") %> prohibited this star from being saved:</h2>

      <ul>
        <% @galaxy.star.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :classification %>
    <%= form.select :classification, ['white dwarf', 'stellar remnant', 'neutron star']%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
